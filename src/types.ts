export interface IChain {
    name: string;
    description: string;
    image: string;
}

export interface IUser {
    id: string;
    name: string;
    address: string;
    phoneNumber: number;
    email: string;
}
