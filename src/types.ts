export interface IChain {
    name: string;
    description: string;
    image: string;
}

export interface IUser {
  id: string;
  email: string;
  address: string;
  name: string;
  phoneNumber: string;
}
